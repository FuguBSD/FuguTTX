[project]
name = "fuguttx"
version = "0.0.0"
description = "FuguTTX workspace root. It holds no application code."
requires-python = ">=3.12"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.12",
]

[tool.ruff]
target-version = "py312"
line-length = 100
